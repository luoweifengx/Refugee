---
description: 新增功能或修改内容时更新更新日志
alwaysApply: true
---

# 更新日志

新增功能，或修改玩家能感知的内容（玩法、对话、配方、分类、界面文案）时，在收尾前更新 `docs/更新日志.md`。

- 写在与 `gradle.properties` 里 `version` 相同的 `## x.y.z` 下。
- 该版本下用「新增」「修改」或「修复」。
- 用中文写玩家能看懂的句子，说明行为变化。一条只写一件事。
- 不要因此改 `gradle.properties` 里的版本号，除非用户要求发布。
